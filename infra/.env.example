# Cloudflare credentials for non-interactive (CI) deploys.
# For local interactive use, prefer `wrangler login` instead.
#
# Copy to `.env` (gitignored) and fill in. `wrangler` reads these from the
# environment automatically.

CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=
